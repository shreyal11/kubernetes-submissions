apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: todo-image-claim
  namespace: project
spec:
  storageClassName: todo-image-storage
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
