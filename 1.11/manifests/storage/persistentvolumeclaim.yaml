apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ping-pong-log-claim
spec:
  storageClassName: ping-pong-log-storage
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
